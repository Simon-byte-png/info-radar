# 音频转写（Whisper）需要 OpenAI key；留空则跳过音频源、只处理文本源
OPENAI_API_KEY=

# 只处理最近 N 天内发布的内容
LOOKBACK_DAYS=14

# 可选：覆盖摘要模型。必须与实际使用的后端匹配：
#   - 用 OpenAI 时填 gpt- 开头的模型（默认 gpt-4o-mini）
#   - 用平台 Anthropic 端点时填 claude 开头的模型（默认 claude-sonnet-5）
# 填错前缀会被忽略并回退到该后端的默认模型，避免把模型名发错家。
# SUMMARY_MODEL=

# 摘要后端优先级：配了 OPENAI_API_KEY 用 OpenAI，否则自动使用平台注入的
# ANTHROPIC_BASE_URL / ANTHROPIC_AUTH_TOKEN。
